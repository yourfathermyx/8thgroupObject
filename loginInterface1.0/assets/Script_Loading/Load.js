
cc.Class({
    extends: cc.Component,

    properties: {
        m_ProMaxLen: cc.Integer,
        m_ProImage:cc.Node,
    },

    setProgress:function(pro){
        
        if(pro > 1 || pr0 < 0){
            return
        }
        var setWith = this.m_ProMaxLen * pro
        
    },

    onLoad () {
        this.m_ProgressMax
    },

    start () {

    },

    // update (dt) {},
});
